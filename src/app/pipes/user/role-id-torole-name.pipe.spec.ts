import { RoleIdToroleNamePipe } from './role-id-torole-name.pipe';

describe('RoleIdToroleNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleIdToroleNamePipe();
    expect(pipe).toBeTruthy();
  });
});
