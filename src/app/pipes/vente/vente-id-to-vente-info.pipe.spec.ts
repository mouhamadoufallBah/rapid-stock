import { VenteIdToVenteInfoPipe } from './vente-id-to-vente-info.pipe';

describe('VenteIdToVenteInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new VenteIdToVenteInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
